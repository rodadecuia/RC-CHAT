# Variáveis de Ambiente para RC-CHAT
# Este é um arquivo de exemplo. Copie para .env e preencha com os valores corretos.

#-- Configurações Gerais --#
TZ=America/Sao_Paulo
EMAIL_ADDRESS=admin@rc-chat.host

#-- Configurações de Host e Porta (Ambiente Local) --#
FRONTEND_HOST=localhost
FRONTEND_PORT=3000
BACKEND_PORT=8080

#-- Configurações de Host (Ambiente de Produção/ACME) --#
# FRONTEND_HOST=rc-chat.exemplo.com.br

#-- URLs --#
BACKEND_PATH=/backend
BACKEND_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}${BACKEND_PATH}
FRONTEND_URL=http://${FRONTEND_HOST}:${FRONTEND_PORT}

#-- URLs (Ambiente Cloudflare) --#
# BACKEND_URL=https://api-rc-chat.example.com
# FRONTEND_URL=https://rc-chat.example.com

#-- Banco de Dados (PostgreSQL) --#
DB_DIALECT=postgres
DB_HOST=postgres
DB_PORT=5432
DB_USER=rc-chat
DB_NAME=rc-chat
DB_TIMEZONE=-03:00

#-- Redis --#
REDIS_URI=redis://redis:6379
REDIS_OPT_LIMITER_MAX=1
REDIS_OPT_LIMITER_DURATION=3000

#-- Limites da Aplicação --#
USER_LIMIT=10000
CONNECTIONS_LIMIT=100000
CLOSED_SEND_BY_ME=true

#-- Integrações --#
RECAPTCHA_SECRET_KEY=
RECAPTCHA_SITE_KEY=

#-- Tokens e Verificações --#
VERIFY_TOKEN=rc-chat
SOCKET_ADMIN=true

#-- Cloudflare Tunnel --#
# TUNNEL_TOKEN=TunnelTokenAsProvidedByCloudflare

#-- Nginx (ACME) --#
# VIRTUAL_HOST=${FRONTEND_HOST}
# VIRTUAL_PORT=80
# LETSENCRYPT_HOST=${FRONTEND_HOST}
# LETSENCRYPT_EMAIL=${EMAIL_ADDRESS}

#-- Frontend --#
BACKEND_HOST=${FRONTEND_HOST}
BACKEND_PROTOCOL=http
# BACKEND_HOST=api-rc-chat.example.com # Para Cloudflare

#-- Armazenamento (Storage) --#
# STORAGE_TYPE=s3 # local ou s3
# S3_BUCKET=
# AWS_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

#-- Notificações Push (Web Push) --#
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

#-- Proxy para Conexão WhatsApp --#
# PROXY_URL=http://user:pass@host:port # ou socks5://user:pass@host:port
# PROXY_TYPE=https-proxy # ou socks-proxy

#-- Ngrok (Desenvolvimento) --#
# NGROK_ENABLED=true
# NGROK_TOKEN=
